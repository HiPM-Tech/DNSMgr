# 已知问题 (Known Issues)

本文档记录当前版本中已知但尚未修复的问题。

## 版本 1.5.0

### 1. 域名解析记录编辑无法正确回显

**严重程度**: 🔴 高

**问题描述**:
- 在域名管理页面编辑 DNS 解析记录时，部分字段无法正确回显已有值
- 特别是 SRV 记录等特殊记录类型，编辑时字段显示为空或默认值
- 用户需要重新输入所有字段值，影响使用体验

**影响范围**:
- 所有 DNS 记录类型的编辑操作
- 特别影响 SRV、MX 等复杂记录类型

**临时解决方案**:
- 编辑前手动记录原有值
- 如果误操作，可以取消编辑后重新打开

**调试进展**:
- 已尝试使用非受控模式重写 RecordForm
- 已添加递增 key 强制组件重新挂载
- 已优化 useFormSync Hook 的初始状态设置
- 正在持续调试中，问题仍未完全解决

**相关文件**:
- `client/src/components/RecordForm.tsx`
- `client/src/hooks/useFormSync.ts`

---

### 2. 系统安全子页部分模块回显异常

**严重程度**: 🟡 中

**问题描述**:
- 在系统设置的安全子页面中，部分表单字段无法正确回显当前配置值
- 用户看到的可能是默认值或空值，而非实际保存的配置
- 可能导致用户误以为配置未保存或配置丢失

**影响范围**:
- 系统设置 > 安全设置页面
- 可能涉及 2FA 配置、信任设备、安全策略等模块

**临时解决方案**:
- 通过 API 直接查询当前配置值
- 修改配置时先查看当前值，避免覆盖

**调试进展**:
- 已使用 placeholderData 确保表单默认值立即显示
- 已修复部分表单默认值回显问题
- 仍有部分模块存在回显异常

**相关文件**:
- `client/src/pages/Settings.tsx`
- `client/src/components/SecuritySettings.tsx` (如存在)

---

## 问题跟踪

这些问题将在后续版本中优先修复。如果您遇到这些问题或有相关建议，欢迎提交 Issue。

**最后更新**: 2026-05-17
**版本**: 1.5.0
